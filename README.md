# Intelion Standalone Agent - Releases

Signed build artifacts for the Intelion Standalone Agent.

## Downloads

Check the [Releases](../../releases) page for the latest signed builds.

### Available Platforms

| Platform | File |
|----------|------|
| Windows  | `*-local-agent-*-setup.exe` |
| macOS    | `*-local-agent-*.pkg` |
| Linux    | `*-local-agent-*.AppImage` |

## Auto-Updates

Each release includes platform-specific manifest files (`latest.yml`, `latest-mac.yml`, `latest-linux.yml`) used by the built-in auto-updater.
